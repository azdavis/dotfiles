#!/usr/bin/env sh

. "$HOME/.config/etc/base.sh"

wd="$HOME/.config/home"
cd "$wd"

touch "$HOME/.hushlogin"
for f in $(find *); do
    dst="$HOME/.$f"
    if [ -d "$f" ]; then
        mkdir -p "$dst"
    else
        ln -sfn "$wd/$f" "$dst"
    fi
done
