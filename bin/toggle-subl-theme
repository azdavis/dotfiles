#!/usr/bin/env sh

. "$HOME/.config/lib/base.sh"
. "$HOME/.config/lib/abort.sh"

cd "$HOME/.config/subl"

alias patch="git apply theme/light.patch"
if patch --check &> /dev/null; then
    patch
elif patch --reverse --check &> /dev/null; then
    patch --reverse
else
    abort "patch does not apply in either direction"
fi
