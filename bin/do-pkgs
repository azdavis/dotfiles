#!/bin/sh

set -eu

brew update
brew bundle --verbose --global || true
brew bundle cleanup --force --global
brew unlink coreutils >/dev/null
brew cleanup

npm i -g npm

tlmgr update --self
tlmgr update --all

rustup self update
rustup update stable
