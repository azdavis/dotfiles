#!/bin/sh

set -eu

brew update
brew upgrade
brew cleanup

rustup self update
rustup update

cd "$HOME/.dotfiles"
mkdir -p site-functions
rustup completions zsh rustup > site-functions/_rustup
rustup completions zsh cargo > site-functions/_cargo
