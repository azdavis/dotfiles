#!/usr/bin/env sh

cd "$(cd "$(dirname "$0")" && pwd -P)"
source "../etc/base.sh"

new_shell="$(brew --prefix)/bin/zsh"

[[ "$new_shell" == "$SHELL" ]] && exit 0

sudo -s "echo $new_shell >> /etc/shells"
chsh -s "$new_shell"
