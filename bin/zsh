#!/usr/bin/env zsh

set -euo pipefail
IFS=$'\n\t'
[[ "$USER" == 'root' ]] && echo 'do not run as root' && exit 1

new_shell="$(brew --prefix)/bin/zsh"

[[ "$new_shell" == "$SHELL" ]] && exit

sudo -s "echo $new_shell >> /etc/shells"
chsh -s "$new_shell"
