#!/usr/bin/env zsh

source "$HOME/.config/etc/base.sh" || exit 1

wd="$HOME/.config/home"
cd "$wd"

touch "$HOME/.hushlogin"
for f in $(find *); do
    if [[ -d "$f" ]]; then
        mkdir -p "$HOME/.$f"
    else
        sym "$wd/$f" "$HOME/.$f"
    fi
done

true
