#!/usr/bin/env zsh

source "$HOME/.config/etc/base.sh" || exit 1

wd="$HOME/.config/home"
cd "$wd"

touch "$HOME/.hushlogin"
for f in $(find *); do
    dst="$HOME/.$f"
    if [[ -d "$f" ]]; then
        mkdir -p "$dst"
    else
        symlink "$wd/$f" "$dst"
    fi
done

true
