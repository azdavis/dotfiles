#!/usr/bin/env sh

cd "$(cd "$(dirname "$0")" && pwd -P)"
source "../etc/base.sh"

wd="$HOME/.config/home"
cd "$wd"

touch "$HOME/.hushlogin"
for f in $(find *); do
    dst="$HOME/.$f"
    if [[ -d "$f" ]]; then
        mkdir -p "$dst"
    else
        symlink "$wd/$f" "$dst"
    fi
done
