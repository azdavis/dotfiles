#!/bin/sh

set -eu
. "$HOME/.config/lib/symlink.sh"

touch "$HOME/.hushlogin" "$HOME/.netrc"
chmod 600 "$HOME/.hushlogin" "$HOME/.netrc"

wd=".config/home"
cd "$HOME/$wd"
for x in *; do
    symlink "$wd/$x" "$HOME/.$x"
done
