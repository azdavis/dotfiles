#!/bin/sh -eu

. ~/.config/lib/force_symlink.sh
. ~/.config/lib/panic.sh

if [ $# -ne 0 ]; then
	panic "usage: $(basename "$0")"
fi

touch ~/.hushlogin
chmod 600 ~/.hushlogin

wd=".config/home"
cd ~/"$wd"
for x in *; do
	force_symlink "$wd/$x" ~/".$x"
done
