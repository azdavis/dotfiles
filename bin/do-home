#!/bin/sh

. "$XDG_CONFIG_HOME/lib/_base.sh"
. "$XDG_CONFIG_HOME/lib/symlink.sh"

if [ $# -ne 0 ]; then
	abort "usage: $(basename "$0")"
fi

touch "$HOME/.hushlogin"
chmod 600 "$HOME/.hushlogin"

wd="$XDG_CONFIG_HOME/home"
cd "$wd"
for x in *; do
	symlink "$wd/$x" "$HOME/.$x"
done
