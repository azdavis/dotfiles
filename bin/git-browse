#!/usr/bin/env sh

. "$HOME/.config/etc/base.sh"

git status > /dev/null || exit 1

remote="$([ $# -ge 1 ] && echo $1 || echo origin)"

url="$(git config "remote.$remote.url" || true)"
[ "$url" ] || abort "cannot browse '$remote': no such remote"

open "$url"
