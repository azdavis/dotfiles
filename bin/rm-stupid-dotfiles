#!/usr/bin/env sh

source "$HOME/.config/etc/base.sh"

./rm-ds-store "$HOME"

cd
rm -rf \
    .bash_history \
    .bundle \
    .gem \
    .ghc \
    .lldb \
    .node_repl_history \
    .npm \
    .oracle_jre_usage \
    .sh_history
