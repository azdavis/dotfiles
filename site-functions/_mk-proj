#compdef mk-proj

local opts=(
	"-f[create <proj_path> even if it already exists]"
	"-g[create a github repository with the given privacy]:privacy:(pub priv)"
	"-t[create a project with the given type]:type:(c js tex)"
	"-v[explain what is being done]"
	"*:files:_files"
)

_arguments -s $opts
