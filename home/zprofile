last="$HOME/.config/update-dotfiles.last"
if [ ! -f "$last" ] \
|| expr "$(date +%s)" - "$(cat "$last")" ">=" 604800 >/dev/null; then
    echo "==> updating dotfiles"
    "$HOME/.config/bin/update-dotfiles"
fi
unset last
