now="$(date +%s)"
if ! last="$(git -C "$HOME/.config" config --int dotfiles.lastUpdate 2> /dev/null)" \
|| [ $((now - last)) -ge 72000 ]; then
    echo "updating dotfiles"
    "$HOME/.config/bin/update-dotfiles"
fi
unset now last
