now="$(date +%s)"
if ! last="$(git -C "$XDG_CONFIG_HOME" config --int dotfiles.lastUpdate 2> /dev/null)" \
|| [ $((now - last)) -ge 72000 ]; then
    echo "updating dotfiles"
    "$XDG_CONFIG_HOME/bin/update-dotfiles"
fi
unset now last
