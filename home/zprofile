if ! v="$(cat "$HOME/.config/update-dotfiles.last" 2>/dev/null)" \
|| ! expr "$v" + 0 = "$v" >/dev/null 2>&1 \
|| expr "$(date +%s)" - "$v" ">=" 86400 >/dev/null; then
    echo "==> updating dotfiles"
    "$HOME/.config/bin/update-dotfiles"
fi
unset v
