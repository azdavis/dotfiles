f="$HOME/.config/last-update"
if [ ! -e "$f" ] \
|| expr "$(date +%s)" - "$(cat "$f")" ">=" 604800 >/dev/null; then
    echo "==> updating dotfiles"
    "$HOME/.config/bin/update-dotfiles"
fi
unset f
